.ooh-hero {
  position: relative;
  width: 100%;
  min-height: 70vh;
  overflow: hidden;
  border-radius: 18px;
}

.ooh-hero__media {
  position: absolute;
  inset: 0;
  background: #000;
}

.ooh-hero__video {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  opacity: 0;
  transition: opacity 700ms ease;
}

.ooh-hero__video.is-active {
  opacity: 1;
}

.ooh-hero__overlay {
  position: relative;
  z-index: 2;
  min-height: 70vh;
  display: flex;
  align-items: flex-end;
  padding: 28px;
  background: linear-gradient(to top, rgba(0,0,0,.70), rgba(0,0,0,0) 60%);
}

.ooh-hero__copy {
  max-width: 820px;
}

.ooh-hero__title {
  margin: 0 0 8px 0;
  font-size: clamp(28px, 3.2vw, 46px);
  line-height: 1.05;
  color: #fff;
  letter-spacing: .02em;
  text-transform: uppercase;
}

.ooh-hero__tagline {
  margin: 0 0 14px 0;
  color: rgba(255,255,255,.85);
  font-size: 16px;
  max-width: 62ch;
}

.ooh-hero__controls {
  display: flex;
  gap: 10px;
  align-items: center;
  margin-bottom: 14px;
}

.ooh-btn {
  display: inline-block;
  padding: 10px 14px;
  border-radius: 12px;
  background: rgba(255,255,255,.12);
  color: #fff;
  text-decoration: none;
  border: 1px solid rgba(255,255,255,.20);
  backdrop-filter: blur(6px);
}

.ooh-btn:hover { background: rgba(255,255,255,.18); }

.ooh-btn--ghost {
  background: transparent;
}

.ooh-hero__dots {
  display: flex;
  gap: 8px;
  padding: 10px 0 0 0;
}

.ooh-dot {
  width: 10px;
  height: 10px;
  border-radius: 999px;
  border: 1px solid rgba(255,255,255,.45);
  background: rgba(255,255,255,.12);
  cursor: pointer;
}

.ooh-dot.is-active {
  background: rgba(255,255,255,.85);
}

@media (prefers-reduced-motion: reduce) {
  .ooh-hero__video { transition: none; }
}
/* Dissolve effect: dim the hero while prologue is open */
.ooh-hero.is-dimmed .ooh-hero__video {
  filter: blur(2px) brightness(0.55);
  transform: scale(1.02);
  transition: filter 600ms ease, transform 600ms ease;
}

/* Prologue overlay */
.ooh-prologue[hidden] { display: none; }

.ooh-prologue.is-open {
  position: fixed;
  inset: 0;
  z-index: 9999;
}

.ooh-prologue__backdrop {
  position: absolute;
  inset: 0;
  background: rgba(0,0,0,.78);
  opacity: 0;
  animation: oohFadeIn 500ms ease forwards;
}

.ooh-prologue__panel {
  position: absolute;
  left: 50%;
  top: 50%;
  width: min(900px, calc(100% - 24px));
  transform: translate(-50%, -48%);
  border-radius: 18px;
  padding: 18px 18px 16px 18px;
  background: rgba(10,12,16,.93);
  border: 1px solid rgba(255,255,255,.12);
  backdrop-filter: blur(10px);
  color: #fff;
  opacity: 0;
  animation: oohRiseIn 520ms ease forwards;
}

@keyframes oohFadeIn { to { opacity: 1; } }
@keyframes oohRiseIn { to { opacity: 1; transform: translate(-50%, -50%); } }

.ooh-prologue__top {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 10px;
}

.ooh-prologue__kicker {
  font-size: 12px;
  letter-spacing: .14em;
  opacity: .85;
  text-transform: uppercase;
}

.ooh-prologue__title {
  margin: 6px 0 10px 0;
  font-size: clamp(22px, 2.4vw, 34px);
  letter-spacing: .04em;
  text-transform: uppercase;
}

.ooh-prologue__body {
  max-height: 52vh;
  overflow: auto;
  padding-right: 6px;
  line-height: 1.55;
  color: rgba(255,255,255,.90);
}

.ooh-prologue__body p {
  margin: 0 0 12px 0;
}

.ooh-prologue__actions {
  display: flex;
  justify-content: space-between;
  gap: 10px;
  margin-top: 14px;
}
